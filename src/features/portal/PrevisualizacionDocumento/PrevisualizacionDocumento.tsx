import { PlaceholderScreen } from "../../../components/PlaceholderScreen";

export function PrevisualizacionDocumento() {
  return (
    <PlaceholderScreen
      titulo="Borrador del documento"
      descripcion="Previsualización del documento antes de confirmarlo."
    />
  );
}
