import { PlaceholderScreen } from "../../../components/PlaceholderScreen";

export function EstadoTramite() {
  return (
    <PlaceholderScreen
      titulo="Estado del trámite"
      descripcion="Línea de tiempo del trámite y estado de cada firmante."
    />
  );
}
