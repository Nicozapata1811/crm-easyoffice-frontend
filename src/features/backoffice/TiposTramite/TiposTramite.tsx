import { PlaceholderScreen } from "../../../components/PlaceholderScreen";

export function TiposTramite() {
  return (
    <PlaceholderScreen
      titulo="Tipos de trámite"
      descripcion="Configuración de los tipos de trámite: campos, validaciones, plantilla, estados, pago y firma."
    />
  );
}
