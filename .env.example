# Base URL the SPA calls. Leave as /api to go through the dev proxy or the
# production reverse proxy, which keeps the session cookie first-party.
VITE_API_BASE_URL=/api

# Origin the dev server proxies /api to.
VITE_BACKEND_ORIGIN=http://localhost:8000
